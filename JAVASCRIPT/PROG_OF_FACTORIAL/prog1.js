/*let num=4;
let fact=1;

for(let i=1;i<=num;i++){
  fact=fact*i;
}
document.write("factorial is ", fact)
*/

//OR

/*let result=1;//This initializes a variable result to 1. This variable will store the factorial value and is initialized to 1 because any number factorial of 0 is 1.
function fact(n){//This declares a function named fact which takes one parameter n, representing the number whose factorial is to be calculated.
  for(i=1;i<=n;i++){//This is a for loop that initializes a variable i to 1, continues the loop as long as i is less than or equal to n, and increments i by 1 in each iteration. This loop will iterate from 1 to n
    result=result*i;// Inside the loop, this statement calculates the factorial incrementally. In each iteration, it multiplies the current value of result by i and updates result with the new value. This way, result accumulates the factorial value as the loop progresses.
  }
  return result;//After the loop completes, the function returns the final value of result, which is the factorial of the input number n.
}
console.log(fact(5));//this line prints the factorial of 5.
*/

